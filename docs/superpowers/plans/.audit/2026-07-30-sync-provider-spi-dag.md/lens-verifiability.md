## Lens: verifiability
## Charter: `.claude/audit-charter.md` (read in full — enforcement map, recurring bug class "green tests, dead runnable artifact", all three verification gotchas). No `CLAUDE.md` / `AGENTS.md` exists in this repo; confirmed by `find` for both names and by directory listing of the repo root. No `docs/map.md`, `docs/conventions/`, or `docs/boundaries/` exists.

**Downstream context:** none was supplied, and the task statement confirms none exists (plan untracked, every task `pending`, no code landed). Nothing below can be STALE; every finding is live against the plan as written.

---

### Grounding table

| Assumption | Where in artifact | Verified at file:line | Verdict |
|---|---|---|---|
| `src/index.ts:110` is the sole real `buildProgram` site and sits inside `require.main === module` | task-bin-spawn body | `packages/pangolin-cli/src/index.ts:109-110` | VERIFIED |
| Built-in + `--dry-run` through the bin prints `(dry-run) subagent <name>` | task-bin-spawn AC 2 | `packages/pangolin-cli/src/sync.ts:28`; live run of current `dist/index.js` printed `(dry-run) subagent demo`, exit 0 | VERIFIED — **and passes pre-change** |
| Unknown provider through the bin prints `unknown --provider 'made-up'` | task-bin-spawn AC 3 | `packages/pangolin-cli/src/providers/index.ts:16-23`; live run of current `dist/index.js` printed `unknown --provider 'made-up' (known: claude-code, stoa)`, exit 1 | VERIFIED — **and passes pre-change** |
| A probe `.mjs` in `mkdtemp(tmpdir())` run with `{ cwd: pkgRoot }` can resolve `@quarry-systems/pangolin-cli/providers` | task-exports impl sketch + AC 2/3 | live `node` run → `ERR_MODULE_NOT_FOUND`. No self-link in `packages/pangolin-cli/node_modules/@quarry-systems/` (6 entries, none `pangolin-cli`); root `node_modules/@quarry-systems` does not exist; `find -path "*/node_modules/@quarry-systems/pangolin-cli"` → empty | **CONTRADICTED** |
| Node self-reference resolves the subpath from a probe placed *inside* the package once `exports` exists | (absent from artifact) | reproduced in scratch: package with `name` + `exports`, probe inside the dir → printed `claude-code`; same probe copied to tmpdir with `cwd` set to the package → `ERR_MODULE_NOT_FOUND` | VERIFIED |
| `grep -c "getSyncProviders" packages/pangolin-cli/test/` produces a per-file result | task-actions AC 5 | live run → `grep: packages/pangolin-cli/test/: Is a directory`, prints `0`, exit 2. With `-rc` it prints 18 lines (one per file, zeros included) | **CONTRADICTED** |
| `test/e2e/mcp-tool-surface.test.ts` is the spawn pattern to mirror | task-bin-spawn body | `test/e2e/mcp-tool-surface.test.ts:36-39` — lives at **repo root** `test/e2e/`, run only via `vitest.e2e.config.ts --config`, i.e. not by `pnpm -r test` | VERIFIED (placement caveat noted below) |
| The four existing loader error literals | task-config-loader AC 5 | `packages/pangolin-cli/src/index.ts:63,68,88,93` — `:68` and `:93` are byte-identical, so there are three distinct strings, not four | VERIFIED |
| Those literals have any existing test pin today | implied by "unchanged" | zero references to `defaultGetClient` / `defaultGetOrchContext` under any `test/`; strategy 1 = symbol grep across `packages/`, `test/`, `examples/` (hits only `src/index.ts`, `dist/index.d.ts`, two comments); strategy 2 = `ls packages/pangolin-cli/test/` — no config/loader test file exists | **NOT-FOUND** |
| After task-actions, nothing in `src/` imports from `providers/index.ts` | task-barrel AC 3 | strategy 1 = import-path grep: only `cmd-capabilities.ts:21` and `cmd-subagent.ts:20`, both repointed by task-actions. strategy 2 = symbol grep for `SyncProvider|SubagentDef|CapabilityBundle` across `packages/pangolin-cli/src/` outside `providers/` → one comment at `sync.ts:17` | VERIFIED |
| `pangolin-cli` has no `tsconfig.test.json` | Context scope guard 1 | `packages/pangolin-cli/` listing (no such file); `tsconfig.json` `"include": ["src/**/*"]` | VERIFIED |
| `pnpm --filter docs-site build` matches the package | task-docs AC 7 | live `pnpm --filter docs-site exec node -e …` → `MATCHED`, exit 0; `pnpm-workspace.yaml:5` | VERIFIED |
| `astro build` resolves all internal links | task-docs AC 7 parenthetical | `docs-site/astro.config.mjs:5,33` — `starlightLinksValidator({ errorOnRelativeLinks: true, errorOnInvalidHashes: true })` is wired into the Starlight plugin list | VERIFIED |
| CI builds before it tests, so `dist/`-dependent tests are current | task-exports AC 5 | `.github/workflows/ci.yml:61-62` (`pnpm -r build`) immediately precedes `:68-69` (`pnpm -r … test`) | VERIFIED |
| A fixture cwd is establishable for the loader test | task-config-loader sketch comment | `packages/pangolin-cli/test/cmd-deploy.test.ts:20,24,84` uses `process.chdir` with an `originalCwd` restore; vitest `^2.1.9` (fork pool default) | VERIFIED |
| `tsc` still emits after TS2345, so the manual mutation in task-bin-spawn is physically runnable | task-bin-spawn AC 4 | `noEmitOnError` absent from `tsconfig.base.json` and from every JSON in the repo (grep) | VERIFIED |

---

### Findings

- **BLOCKING** · `task-bin-spawn`'s two automated assertions both pass, verbatim, against the current unmodified `dist/index.js` — the task is green before any of the feature exists, and stays green under the one repair the plan forbids.
  - Artifact text: "A built-in provider with `--from <fixture> --dry-run` exits 0 and prints `(dry-run) subagent`." / "Positive control: an unknown provider exits non-zero with `unknown --provider 'made-up'`, proving the run reached resolution rather than dying earlier." / task body: "Pin `src/index.ts:110` by spawning the built binary".
  - Evidence: I executed both cases against the checked-in, pre-change `packages/pangolin-cli/dist/index.js`. Case 1 printed `(dry-run) subagent demo`, exit 0. Case 2 printed `unknown --provider 'made-up' (known: claude-code, stoa)`, exit 1 — because the *old* `resolveProvider` at `packages/pangolin-cli/src/providers/index.ts:16-23` already emits that exact string. Neither assertion touches `getSyncProviders`: `resolveProviderLazily` short-circuits on `findBuiltIn` before `getExtra` (plan, `registry.ts` sketch lines "const builtIn = findBuiltIn(name); if (builtIn) return builtIn;"), and the unknown-provider path produces the same message whether `getExtra` returned `null` or the seam was never wired at all.
  - Concrete failure: two distinct ones, either sufficient.
    (a) **Forbidden-repair blindness.** An implementer under pressure from the red `test/cmd-subagent.test.ts:129` writes `resolveProviderLazily(opts.provider, ctx.getSyncProviders ?? (async () => null))` — which typechecks with an optional member, needs no fixture widening, and leaves *both* bin-spawn cases green. `src/index.ts:110` is then unwired, `--provider remora` reports `unknown --provider 'remora'` against a perfectly valid config, and every gate is green. That is round-1 finding B2 restored, and it is the charter's named recurring bug class ("green tests, dead runnable artifact", instance 2, which cites this exact line).
    (b) **Stale-`dist/` blindness.** `task-bin-spawn`'s `files:` list is one test file; its implementer changes no `src/`. Run locally without a rebuild, the suite passes against a `dist/` that predates task-actions entirely — charter verification gotcha 3. AC 5 guards only *absence* of `dist/index.js`, never staleness. CI is safe (`ci.yml:61-69`); the local loop is not.
  - Resolution: add a third spawn case that is red today and red under every forbidden repair — write a real `pangolin.config.mjs` into the child's cwd exporting `syncProviders = [ /* minimal shape-valid provider named 'probe' */ ]`, then assert `subagent sync --provider probe --from <fixture> --dry-run` exits 0. That is the only assertion in the whole plan that binds the producer (`defaultGetSyncProviders`, task-config-loader) to the consumer (`resolveProviderLazily`, task-registry) through `:110`. As the plan stands, every test fakes one half: task-registry fakes `getExtra`, task-config-loader tests `defaultGetSyncProviders` in isolation, task-actions fakes the seam. **The headline user path — register an out-of-tree provider in `pangolin.config` and resolve it by name — is exercised end to end nowhere.**

- **BLOCKING** · `task-exports`'s subprocess probe cannot resolve the specifier it tests; it fails with `ERR_MODULE_NOT_FOUND` whether the `exports` map is right, wrong, or absent, and the cheapest repair silently voids the check.
  - Artifact text: "const probe = join(await mkdtemp(join(tmpdir(), 'pangolin-spi-')), 'probe.mjs'); … `import { ClaudeCodeProvider } from '@quarry-systems/pangolin-cli/providers';` … const { stdout } = await run(process.execPath, [probe], { cwd: pkgRoot });"
  - Evidence: I ran exactly this shape — a `.mjs` in `mktemp -d` importing `@quarry-systems/pangolin-cli`, with cwd set to `packages/pangolin-cli` — and got `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@quarry-systems/pangolin-cli' imported from C:\…\Temp\tmp.…\probe.mjs`. Node resolves bare specifiers from the *importing file's* directory, not from cwd; `cwd: pkgRoot` is inert here. Two independent negatives confirm nothing is reachable: (1) `packages/pangolin-cli/node_modules/@quarry-systems/` contains six symlinks, none named `pangolin-cli` (no pnpm self-link); (2) root `node_modules/@quarry-systems` does not exist, and `find -maxdepth 4 -path "*/node_modules/@quarry-systems/pangolin-cli"` returns nothing.
  - Concrete failure: the implementer hits a red test that is red for a reason unrelated to the thing under test, and repairs it the cheapest way — a relative `../dist/providers/index.js` import, or an `--experimental` resolver flag, or dropping the subprocess. Each of those stops exercising the `exports` map, which is the entire subject of the task; a missing or misspelled `"./providers"` entry then ships green. AC 4 ("Importing a registry internal (e.g. `mergeProviders`) from the subpath fails") compounds it: a bare "must throw" assertion is satisfied by `ERR_MODULE_NOT_FOUND` just as well as by the intended `SyntaxError: The requested module … does not provide an export named 'mergeProviders'`, so that bullet passes today, against a package with no `exports` map at all.
  - Resolution: place the probe **inside** `packages/pangolin-cli/` (e.g. a gitignored `.probe-<rand>.mjs`, or a temp subdir under the package root) and rely on Node's package **self-reference**, which activates only when `package.json` has an `exports` field — making the resolution itself a test of the map. I verified this: a scratch package with `name: '@quarry-systems/fake-cli'` and the plan's exact `exports` shape resolved `'@quarry-systems/fake-cli/providers'` from a probe inside the directory and printed `claude-code`; the identical probe copied to tmpdir failed. Also pin the negative to the *specific* error (assert the message names `mergeProviders`, or assert the same probe file succeeds for `ClaudeCodeProvider` as its control) so it cannot pass on a resolution failure.

- **DEFERRED** · `task-actions`'s `grep -c` bullet is not executable as written and has no reading under which it produces the claimed result.
  - Artifact text: "`grep -c \"getSyncProviders\" packages/pangolin-cli/test/` counts exactly the two files in this task's `files:` — the other ten `CliContext`-constructing test files are untouched."
  - Evidence: run verbatim in this repo, `grep -c` on a directory without `-r` prints `grep: packages/pangolin-cli/test/: Is a directory`, emits `0`, and exits 2. Adding `-r` does not rescue it: `grep -rc` prints one `path:count` line per file including zeros — 18 lines for this directory (16 test files plus `test/providers/{claude-code,stoa}.test.ts`) — so the output is never "two". `-c` also counts matching *lines*, not sites, so it cannot distinguish "one construction widened" from "all seven `attachSubagentCmd` sites in `cmd-subagent.test.ts` widened" (`test/cmd-subagent.test.ts:15,32,53,67,92,114,126`). Beyond mechanics it is a pure diff property with no control: it cannot tell "the other ten files untouched" from "the other ten files rewritten in ways that happen not to mention `getSyncProviders`". The repo's primary shell is PowerShell (per the environment), where `grep` is not present at all.
  - Concrete failure: the reviewer runs the bullet, gets exit 2 and `0`, and either waves the criterion through or silently substitutes a different command — so the scope-creep guard the bullet exists to enforce is enforced by nobody.
  - Resolution: DEFERRED rather than BLOCKING because scope guard 1 in `## Context` already states the same constraint in prose and names the two in-scope files explicitly, so the *intent* reaches the implementer even with the bullet broken. Replace the bullet with a reviewable diff statement — "`git diff --name-only` for this task lists exactly the four paths in `files:`" — which is checkable, platform-neutral, and actually expresses the property.

- **DEFERRED** · `task-bin-spawn`'s manual mutation step is not a check: unobservable, unrecorded, unreproducible, and the way it will most likely be "performed" observes the compiler rather than the test.
  - Artifact text: "Removing `getSyncProviders` from the `buildProgram` call at `src/index.ts:110` makes this test fail. Verify by hand once during implementation; do not commit the broken state."
  - Evidence: nothing in the repo can record that this was done — the instruction explicitly forbids committing the state that would demonstrate it, and `.github/workflows/ci.yml` has no mutation step. There is also a trap in performing it: `src/index.ts:110` is inside `src/`, which `tsconfig.json` includes, so removing a required member is TS2345 and `pnpm -r build` reports failure. `noEmitOnError` is absent from `tsconfig.base.json` and from every JSON in the repo, so `tsc` still writes `dist/`, meaning the experiment *is* runnable — but only by ignoring a failing build. An implementer who runs `pnpm build`, sees it go red, and concludes "detected" has observed the compiler, not the spawn test, and will have learned nothing about the test. Finding 1 shows what they would have learned: the test does not fail on the naked omission's *behavioral* consequence in case 1 at all, only in case 2.
  - Concrete failure: the plan's sole stated evidence that the bin-spawn test guards `:110` is a step no reviewer can confirm happened and which, if skipped, leaves finding 1 undetected.
  - Resolution: replace with a committed assertion that is red under the un-wired state — the config-provider spawn case in finding 1's resolution. If a mutation check is genuinely wanted, it belongs as a recorded artifact (a `describe.skip`'d fixture, or a note in the task's completion record naming the observed failure message), not as an instruction to break and unbreak the tree.

- **DEFERRED** · `task-config-loader` asks for four "existing" error literals to be pinned but writes only two of them into the plan, and no pre-existing test pins any of the four.
  - Artifact text: "`defaultGetClient` and `defaultGetOrchContext` throw their four **existing** message strings unchanged. Assert them against literals written into the test, not imported from source: `pangolin-cli: no pangolin.config.{ts,js,mjs} found in <cwd>`, and `pangolin-cli: <filename> must export an PangolinClient instance as default or named 'client'`, and the two `orch` equivalents."
  - Evidence: the two quoted strings match `src/index.ts:68` and `:63` exactly (including the "an PangolinClient" typo). The unquoted pair lives at `:88` (`pangolin-cli: ${filename} must export an OrchContext as a named 'orch' export for pangolin orch commands`) and `:93` — and `:93` is byte-identical to `:68`, so there are three distinct strings, not four. There is no existing pin: grep for `defaultGetClient|defaultGetOrchContext|defaultGetSyncProviders` across `packages/`, `test/`, and `examples/` hits only `src/index.ts`, the generated `dist/index.d.ts:14-15`, and two comments; a filename sweep of `packages/pangolin-cli/test/` shows no config or loader test exists. The plan's own guard — "written into the test, not imported from source" — does not prevent transcription from *post-refactor* source, which is what an implementer will do for the two strings the plan did not write down.
  - Concrete failure: the consolidation reworders `defaultGetOrchContext`'s message (e.g. drops "for pangolin orch commands"), the test is transcribed from the new source, review passes because the reviewer has nothing in the plan to compare against, and a user-facing string documented at `docs-site/src/content/docs/reference/config.md` drifts unnoticed. The spec (§7) made "error strings are unchanged" a named requirement precisely to prevent this.
  - Resolution: write all three distinct literals into the plan verbatim, and note that `:68` and `:93` are the same string so the criterion's "four" is really three.

- **DEFERRED** · `task-barrel`'s "the three types remain exported as types" has no check that can observe it, while the value side got a whole-set equality specifically to prevent silent drift.
  - Artifact text: "The three types (`SyncProvider`, `SubagentDef`, `CapabilityBundle`) remain exported as types." — alongside "asserted as a whole-set equality on `Object.keys`, so a later addition fails the test rather than passing silently."
  - Evidence: `Object.keys(barrel)` cannot see `export type` — types erase, so the sketch test at `test/providers-barrel.test.ts` is structurally blind to this bullet. The compiler cannot see it either: after task-actions repoints `cmd-subagent.ts:20` and `cmd-capabilities.ts:21` to `./providers/registry.js`, **nothing in `packages/pangolin-cli/src/` imports from `providers/index.ts`** — verified two ways: (1) import-path grep over `src/` and `test/` finds only those two lines; (2) symbol grep for the three type names across `src/` outside `providers/` finds only a prose comment at `sync.ts:17`. And `pangolin-cli` has no `tsconfig.test.json` (charter enforcement map; confirmed by directory listing and `tsconfig.json`'s `"include": ["src/**/*"]`), so no fixture can pin it either. Deleting the `export type { … }` line would leave `pnpm -r build`, `pnpm -r typecheck`, `pnpm -r lint`, and the barrel test all green.
  - Concrete failure: the published SPI loses the only thing an out-of-tree author needs for `implements SyncProvider`, and every gate stays green. `task-exports`'s probe imports only the *value* `ClaudeCodeProvider`, so it does not cover the gap either.
  - Resolution: add a type-level assertion the compiler will actually run — the simplest is a line in the `task-exports` probe's TypeScript counterpart, or a `test/providers-barrel.test.ts` block containing `import type { SyncProvider, SubagentDef, CapabilityBundle } from '../src/providers/index.js';` plus a trivial `const _c: SyncProvider = new ClaudeCodeProvider();`. Note this only bites if the file is type-checked, which requires the package to gain a `tsconfig.test.json` — otherwise state plainly that this criterion is review-only.

---

### Checked, no finding

- **`task-registry`'s laziness pair is correctly built.** The negative ("never invokes `getExtra`", asserted with a throwing fake) is explicitly paired with a positive companion ("a config-supplied name **does** invoke it and resolves to the config provider, so the negative cannot pass on a no-op"). This is exactly the structure that defeats "passes on a crash" — the negative alone would pass if resolution threw on line 1; the positive proves the observation channel works.
- **`task-registry`'s `source`-naming test is genuinely non-tautological.** `mergeProviders([{ name: 'remora' }], 'pangolin.config.js')` passes the filename *in* as a parameter, so the assertion on `pangolin.config.js` in the message can only pass if the value is threaded through `validateEntry`'s template. A hardcoded `'pangolin.config.mjs'` fails it. This is spec §9's load-bearing row and the plan carries it correctly.
- **The `unknown --provider` regexes survive rewording because the wording is already fixed in landed code.** `packages/pangolin-cli/src/providers/index.ts:20` emits it today and `test/cmd-subagent.test.ts:131` already asserts `/unknown --provider 'made-up'/`. The plan's `registry.ts` sketch reproduces the string exactly. These are pins on existing behavior, not constants compared to themselves.
- **`task-actions` AC 4 correctly demands a pair, not just a negative,** for the capabilities side ("its config-provider counterpart **does** invoke the seam — the capabilities side gets the same negative/positive pair as the subagent side, not just the negative"). That closes the gap spec §4.5 flagged where `cmd-capabilities.test.ts` passes only incidentally.
- **`task-barrel`'s value-export check is the right shape.** Whole-set `Object.keys(...).sort()` equality, not `toContain` — a later addition fails rather than passing silently, as the criterion states.
- **`task-exports`'s in-process prohibition is correct reasoning.** "an in-process `await import()` observes vitest's resolver and never exercises `cjs-module-lexer`, so it would prove nothing" is right, and matches spec §3.1. Only the probe's *placement* is wrong (finding 2); the requirement itself should be kept.
- **`task-docs`'s build gate is real.** `pnpm --filter docs-site build` matches the package despite the `@pangolin/` scope (verified by running `pnpm --filter docs-site exec`), `docs-site` is in `pnpm-workspace.yaml:5` so `pnpm -r build` covers it too, and the parenthetical "astro build resolves all internal links" is accurate — `docs-site/astro.config.mjs:33` wires `starlightLinksValidator({ errorOnRelativeLinks: true, errorOnInvalidHashes: true })`. The task is also honest that the content criteria are review-only.
- **`task-changelog` correctly reports the absence of a check** rather than inventing one: "Test file: none — `CHANGELOG.md` has no automated check; verified by review against the criteria above." Its four criteria are concrete and reviewable, and the cited convention is real (`CHANGELOG.md:12` is a `### Breaking` heading under `## [0.4.0]`). This is the right handling of an unautomatable task.
- **No task requires absent infrastructure.** Nothing here needs docker, a live DB, or network. The only environmental dependency is a current `dist/` for `task-bin-spawn` and `task-exports`; CI satisfies it (`ci.yml:61-62` build immediately precedes `:68-69` test), and `task-bin-spawn` AC 5 correctly requires a clear message when `dist/index.js` is *absent*. The staleness gap is covered in finding 1(b).
- **The config-loader fixture cwd is achievable** — my initial concern that `process.chdir` might be unavailable under vitest's worker pool is unfounded: vitest is `^2.1.9` (fork pool default) and `packages/pangolin-cli/test/cmd-deploy.test.ts:20,24,84` already uses `process.chdir` with an `originalCwd` restore. The plan's sketch leaves the mechanism as a comment rather than code, but a working in-repo idiom exists two files away.

### Out of lens

- `task-bin-spawn` places the new spawn test at `packages/pangolin-cli/test/`, while the pattern it says it mirrors lives at repo-root `test/e2e/` and is deliberately excluded from `pnpm -r test` (`vitest.e2e.config.ts` header). The placement is arguably better — it actually runs in CI — but the plan does not acknowledge the divergence.
- The `providers-registry.test.ts` sketch imports `describe` and never uses it; `pnpm -r lint` runs `eslint src test` for this package (`package.json:29`).
