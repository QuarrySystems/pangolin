## Lens: dag-integrity
## Charter: read `.claude/audit-charter.md` in full (enforcement map, recurring bug classes,
verification gotchas). No `CLAUDE.md`/`AGENTS.md` exists in this repo (confirmed absent —
checked repo root directly, no other lens dependency). Also read
`docs-site/src/content/docs/reference/package-map.md` for the pangolin-mcp/pangolin-cli
package-graph boundary the plan's D10 relies on.

No downstream artifacts were supplied beyond the parent spec; the plan itself is untracked
(`??`) and every task is `status: pending`, so there is no landed code or dispatched task to
check findings against — all findings below are evaluated directly against the plan and the
current repo tree.

### Grounding table
| Assumption | Where in artifact | Verified at file:line | VERIFIED / CONTRADICTED / NOT-FOUND / UNVERIFIABLE |
|---|---|---|---|
| Mermaid edges match `depends_on:` fields exactly | lines 20-26 vs. each task's `depends_on:` block | plan lines 20-26, 92,246,355,419,472,542,606,677 | VERIFIED — 7 edges in the diagram, 7 non-empty `depends_on:` entries across 8 tasks, one-to-one, no extra/missing edge, no cycle |
| "44 construction sites across 12 test files" (`getClient:`) | line 55 | grep `getClient:` in `packages/pangolin-cli/test/**` → 44 occurrences, **11** files (not 12): cmd-orch-schedule, integration, cmd-subagent, bin-entry, cmd-pipeline, cmd-deploy, cmd-verify, cmd-env, cmd-orch, cmd-dispatch, cmd-capabilities `.test.ts` | CONTRADICTED on file count (44 confirmed, 12 files not found — only 11); see Findings |
| Exactly one test reaches the new seam at runtime (`test/cmd-subagent.test.ts:129`, `--provider made-up`) | lines 61-65 | `packages/pangolin-cli/test/cmd-subagent.test.ts:124-132` (the `made-up` case is at :129, assertion at :131); confirmed via a repo-wide sweep for `--provider` usage (two independent strategies: grep `--provider` across `packages/pangolin-cli/test/**`, and grep `'sync'` usage across `packages/pangolin-cli/test/**`) that only `cmd-subagent.test.ts` and `cmd-capabilities.test.ts` ever invoke the `sync` subcommand, and only the former uses a non-built-in name | VERIFIED |
| `cmd-capabilities.test.ts` passes "incidentally" (only ever passes `claude-code`) | plan lines 427-429 (quoting spec) | `packages/pangolin-cli/test/cmd-capabilities.test.ts:21,39,63` — all three `--provider` args are `claude-code` | VERIFIED |
| No other in-repo consumer of `resolveProvider`/`mergeProviders`/`findBuiltIn`/`PROVIDERS`/`listProviderNames` exists outside `cmd-subagent.ts`/`cmd-capabilities.ts` | task-actions, task-barrel rationale | repo-wide grep (excluding node_modules) for those five symbols → only `providers/index.ts` (definer), `cmd-subagent.ts`, `cmd-capabilities.ts`, plus doc/spec/audit prose | VERIFIED |
| `pangolin-mcp` has no package-graph edge to `pangolin-cli` (D10) | spec §7, plan line 76-78 | `docs-site/src/content/docs/reference/package-map.md:41-50` — only `pangolin-client`/`pangolin-core` are shared; `pangolin-cli` and `pangolin-mcp` are parallel leaves | VERIFIED |
| `CliContext`/`buildProgram` are constructed only inside `packages/pangolin-cli/test/**` | scope guard 1 framing (implicit) | repo-wide grep for `buildProgram(` / `CliContext` found two additional construction sites: `test/e2e/manifest-deploy.test.ts:187,189` and `examples/manifest/test/deploy.test.ts:145-146`, both passing `{ getClient: ... }` only | CONTRADICTED as a literal scope claim, but see Findings — neither reaches the new seam |
| `CHANGELOG.md:12` is a `### Breaking` heading (task-changelog's anchor) | plan lines 703-704 | `CHANGELOG.md:10-12` — `## [0.4.0] - 2026-07-28` then `### Breaking` at line 12 | VERIFIED |
| All declared `files:` paths for existing-file tasks resolve on the tree; all "new file" claims are genuinely absent today | every task's `files:` block | `packages/pangolin-cli/src/index.ts`, `cmd-subagent.ts`, `cmd-capabilities.ts`, `providers/index.ts`, `package.json` all exist; `providers/registry.ts`, `test/providers-registry.test.ts`, `test/config-loader.test.ts`, `test/providers-barrel.test.ts`, `test/bin-spawn.test.ts`, `test/exports-resolution.test.ts` all absent from the current tree (confirmed via `Glob packages/pangolin-cli/**/*` and `Glob packages/pangolin-cli/test/*.test.ts`); doc targets `sync-capabilities-subagents.md`, `config.md`, `cli.md` all exist | VERIFIED |
| Contract surfaces (`ConfigProviders`, `resolveProviderLazily`, `CliContext.getSyncProviders`) are only consumed by tasks ordered after their producer | task-config-loader/task-actions implementation blocks | `packages/pangolin-cli/src/cmd-subagent.ts:20`, `cmd-capabilities.ts:21` currently import `resolveProvider` from `./providers/index.js`; plan's task-actions switches both to `./providers/registry.js`, correctly gated behind `depends_on: [task-config-loader]` → `[task-registry]` | VERIFIED |

### Findings

- **DEFERRED** · Scope guard 1's file count ("12 test files") is wrong; the underlying execution-safety conclusion is right.
  - Artifact text: "A grep for `getClient:` finds **44 construction sites across 12 test files**." (line 54-55)
  - Evidence: `Grep pattern:"getClient:" path:packages/pangolin-cli/test` returns 44 matches across exactly **11** files (`cmd-orch-schedule.test.ts:1, integration.test.ts:1, cmd-subagent.test.ts:6, bin-entry.test.ts:1, cmd-pipeline.test.ts:5, cmd-deploy.test.ts:7, cmd-verify.test.ts:1, cmd-env.test.ts:11, cmd-orch.test.ts:2, cmd-dispatch.test.ts:7, cmd-capabilities.test.ts:2`), sum = 44. Two independent negative checks that no 12th file exists: (1) bare `getClient` (no colon) over the same directory returns the identical 11-file set; (2) `providers/claude-code.test.ts`, `providers/stoa.test.ts`, `sync.test.ts`, `frontmatter.test.ts`, `manifest-parser.test.ts`, `scaffold-shape.test.ts`, `cmd-orch-audit-evidence.test.ts` — the seven remaining `.test.ts` files in the package — were individually read/grepped and none constructs a `CliContext` or calls `buildProgram`/`attachSubagentCmd`/`attachCapabilitiesCmd`.
  - Concrete failure: none for DAG execution. This is the artifact's own evidence citation being off by one file, not a race the tripwire misses — I independently re-derived the load-bearing conclusion (exactly one test reaches the new seam) by a different method (grepping every `--provider` invocation in the package, not counting `getClient:` sites) and it holds. The `task-actions` `files:` list (`cmd-subagent.ts`, `cmd-capabilities.ts`, and their two test files) is therefore not understated for the runtime-break question the scope guard exists to answer.
  - Resolution: cosmetic — correct "12" to "11" in the scope guard, or state the grep explicitly excludes `test/providers/*` and other non-CliContext-constructing files if a broader count was intended.

- **DEFERRED** · Two `CliContext`-shaped construction sites exist outside `packages/pangolin-cli/test/` that the scope guard's framing doesn't name, though neither reaches the new seam.
  - Artifact text: "**The other ten test files are out of scope.** Touching them is scope creep, not thoroughness." (lines 64-65) — framed as if the pangolin-cli test directory is the complete universe of `CliContext` construction.
  - Evidence: `test/e2e/manifest-deploy.test.ts:187` (`const ctx = { getClient: async () => fake as any };`) then `:189` (`buildProgram(ctx)`), run via `pnpm test:e2e` → `.github/workflows/e2e.yml:59-60`, which is gated on every PR/push to main and is **not** one of the four commands the plan's own Verification section lists (`pnpm -r build`, `pnpm -r lint`, `pnpm -r typecheck`, `pnpm -r --workspace-concurrency=1 test`). Also `examples/manifest/test/deploy.test.ts:145-146` (same shape), which appears to be dead/unexecuted — `examples/manifest` has no `package.json` (confirmed via `Glob examples/manifest/package.json` → no match, and directory listing shows no `package.json`), so it isn't a pnpm workspace package and isn't in `vitest.e2e.config.ts`'s `include: ['test/**/*.test.ts']` (root-relative only). Both sites call `buildProgram`/`attachDeployCmd` and drive only the `deploy` subcommand, whose action (`packages/pangolin-cli/src/cmd-deploy.ts:40-75`) never reads `ctx.getSyncProviders` — confirmed by reading the full action body.
  - Concrete failure: none. Neither test invokes `subagent sync`/`capabilities sync`, so neither reaches `resolveProviderLazily` and neither breaks when `CliContext` gains a required third member (the missing member is simply never dereferenced). This does not contradict the plan's runtime-break conclusion, but it does mean the scope guard's implicit universe ("packages/pangolin-cli's own test files") isn't literally the complete set of `CliContext` construction sites in the repo, and the plan's Verification section omits `pnpm test:e2e` even though CI gates on it.
  - Resolution: no task change required (both sites are correctly untouched). Optionally note in the scope guard that the sweep was package-scoped, and optionally add `pnpm test:e2e` to the Verification section for completeness — the latter is a coherence/absence-lens concern, noted here only because it surfaced during the same grounding sweep.

### Checked, no finding

- **Mermaid ↔ `depends_on:` correspondence**: all 7 edges in the flowchart (`task-registry→task-config-loader`, `task-config-loader→task-actions`, `task-config-loader→task-changelog`, `task-actions→task-barrel`, `task-actions→task-bin-spawn`, `task-barrel→task-exports`, `task-barrel→task-docs`) exactly match the 7 non-empty `depends_on:` arrays across the 8 task frontmatter blocks. No cycle; a valid topological order exists (registry → config-loader → {actions, changelog} → {barrel, bin-spawn} → {exports, docs}).
- **No task references a task id absent from the mermaid diagram or vice versa** — 8 nodes, 8 task blocks, ids match 1:1.
- **File-scope races**: cross-checked every pair of tasks that can run concurrently (task-changelog ∥ {actions, barrel, bin-spawn, exports, docs}; task-bin-spawn ∥ task-barrel; task-bin-spawn ∥ {task-exports, task-docs}; task-exports ∥ task-docs) against their declared `files:` lists — no shared path in any pair.
- **`files:` vs. tree**: every existing-file claim resolves (`src/index.ts`, `cmd-subagent.ts`, `cmd-capabilities.ts`, `providers/index.ts`, `package.json`, the three doc pages, root `CHANGELOG.md`); every new-file claim is genuinely absent today (`providers/registry.ts`, `test/providers-registry.test.ts`, `test/config-loader.test.ts`, `test/providers-barrel.test.ts`, `test/bin-spawn.test.ts`, `test/exports-resolution.test.ts`).
- **Contract-surface edges**: `ConfigProviders` (defined task-registry, consumed task-config-loader — edge present); `resolveProviderLazily`/`registry.js` import path (defined task-registry, consumed task-actions — transitively ordered); `CliContext.getSyncProviders` (defined task-config-loader, consumed task-actions and read by task-bin-spawn's wiring test — both correctly ordered after task-config-loader).
- **`task-actions → task-bin-spawn` is a real edge, not a false one**: verified that `--provider claude-code --dry-run` and `--provider made-up` would both already pass against the *pre-task-actions* code (old synchronous `resolveProvider` also enumerates built-ins and throws the same message), so bin-spawn's value — pinning that `:110`'s wiring is actually *exercised* by the action, not merely present — only holds once task-actions has switched `cmd-subagent.ts` to call `resolveProviderLazily(opts.provider, ctx.getSyncProviders)`. Removing the edge would make the test pass vacuously without covering the new seam.
- **`task-actions → task-barrel` ordering**: confirmed both `cmd-subagent.ts:20` and `cmd-capabilities.ts:21` currently import `resolveProvider` from `./providers/index.js`; if `task-barrel` ran first, the build would break. The declared order prevents this.
- **D10 (no `pangolin-mcp`→`pangolin-cli` edge)**: confirmed against `docs-site/src/content/docs/reference/package-map.md` — no fallout task needed, none exists.
- **No `single_threaded:` field is declared anywhere, and no task has an empty `files:` list** — the two conditions my lens is asked to check together don't arise.
- **`is_wiring_task:`/`review_mode:`/`model_hint:` frontmatter fields** are consistent with the schema used across ~30 other DAG plans in `docs/superpowers/plans/`, not a one-off malformed field.

### Out of lens
- The Verification section's four commands (`pnpm -r build/lint/typecheck/test`) omit `pnpm test:e2e`, which `.github/workflows/e2e.yml` runs on every PR/push to main — worth a coherence/absence-lens look, though I confirmed it would pass regardless (see Findings).
