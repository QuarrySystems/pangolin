## Lens: context-sufficiency
## Charter: `.claude/audit-charter.md` (read in full). No `CLAUDE.md`/`AGENTS.md` exists in this repo — confirmed by the task brief and by a filename sweep; no `docs/conventions/*` or `docs/boundaries/*` either. `RELEASING.md`, `.github/workflows/ci.yml`, and `.github/workflows/typecheck.yml` were read as the repo's actual enforcement surface.

**Downstream context:** none was given and none exists — the plan is untracked, all eight tasks are `status: pending`, no code has landed. Every finding below is therefore live rather than inert; none can be graded STALE.

**Execution contract audited against:** each task body + its immediate dependencies' *landed output* (i.e. repo source, which an implementer can read) — but **not** the spec, the charter, `## Context`, or any sibling task body.

### Grounding table

| Assumption | Where in artifact | Verified at file:line | Verdict |
|---|---|---|---|
| `src/index.ts:110` is the sole real `buildProgram` construction site, inside a `require.main` guard | task-config-loader AC 6; task-bin-spawn body | `packages/pangolin-cli/src/index.ts:109-110`; only other `buildProgram` callers are tests (`test/bin-entry.test.ts:6`, `test/integration.test.ts:117`) and `examples/manifest/test/deploy.test.ts:145` | VERIFIED |
| The four existing error literals live at `index.ts:63,68,88,93` | task-config-loader AC 5 | `packages/pangolin-cli/src/index.ts:63,68,88,93` | VERIFIED |
| `providers/index.ts` currently holds the registry and is imported only by the two `cmd-*` files | task-barrel body | `src/providers/index.ts:11-27`; importers: `src/cmd-subagent.ts:20`, `src/cmd-capabilities.ts:21` (grep `resolveProvider\|listProviderNames` across `packages/ examples/ deploy/`, excluding `dist/`) | VERIFIED |
| `splitFrontmatter`, `SyncProvider`, `SubagentDef`, `CapabilityBundle` exist to be re-exported by the trimmed barrel | task-barrel implementation | `src/frontmatter.ts:17`; `src/providers/types.ts:19,33,38` | VERIFIED |
| `cmd-subagent.test.ts` `--provider made-up` is the one fixture that reaches the seam | task-actions body | `packages/pangolin-cli/test/cmd-subagent.test.ts:124-133` (construction at `:126`, `made-up` at `:129`, assertion at `:131`) | VERIFIED |
| `pangolin-cli` has no `tsconfig.test.json`; `tsconfig.json` includes `src/**/*` only | `## Context` guard 1; `## Verification` | `packages/pangolin-cli/tsconfig.json:6`; `ls -a packages/pangolin-cli/` shows no `tsconfig.test.json` | VERIFIED |
| `pnpm -r build` precedes `pnpm -r test` in CI, so `dist/` is current for the spawn tests | task-exports AC 5; task-bin-spawn AC 5 | `.github/workflows/ci.yml:44,62,68` | VERIFIED |
| `CHANGELOG.md:12` is the `### Breaking` heading of the "existing convention" | task-changelog AC 1 | `CHANGELOG.md:10` is `## [0.4.0] - 2026-07-28`; `:12` is that **released** section's `### Breaking`. No `## [Unreleased]` heading exists (heading scan of `^## `: 10, 83, 99, 240) though `RELEASING.md:13-14` says entries live there | **CONTRADICTED** (see F1) |
| `test/e2e/mcp-tool-surface.test.ts` is a pattern the task-bin-spawn implementer can find | task-bin-spawn body | Actual path is repo-root `./test/e2e/mcp-tool-surface.test.ts`, **not** `packages/pangolin-cli/test/e2e/...`; `find . -name "mcp-tool-surface*"` returns only that and a worktree copy | CONTRADICTED (minor; noted under F-checked) |
| `cli.md` has a `sync` row and a `--provider` row | task-docs AC 6 | `docs-site/src/content/docs/reference/cli.md:37` and `:49` are both `sync` rows; no `--provider` row exists (grep `--provider` in cli.md → 2 hits, both those rows) | **CONTRADICTED** (see F7) |
| A tmpdir `.mjs` probe can resolve `@quarry-systems/pangolin-cli/providers` given `cwd: pkgRoot` | task-exports implementation | No `node_modules/@quarry-systems/` at repo root; `packages/pangolin-cli/node_modules/@quarry-systems/` holds only its four workspace deps, no self-link | **CONTRADICTED** (see F8) |
| `loadSubagents(dir)` has two conflicting meanings (task-docs must state them) | task-docs AC 4 | `src/providers/claude-code.ts:28,31-32` (dir = the agents directory) vs `src/providers/stoa.ts:33,38-40` (`defaultSubagentDir = '.'`, dir = repo root) — true, but stated nowhere the implementer can read | VERIFIED-as-fact / NOT-FOUND-in-task (see F6) |
| `process.chdir` fixtures are viable for the config-loader test | task-config-loader implementation comment | Prior art at `packages/pangolin-cli/test/cmd-deploy.test.ts:24,84,116,158,200,233,257` | VERIFIED |

### Findings

- **BLOCKING** · `task-changelog` is undecidable as written: its only placement anchor points inside an already-released version section, and no `[Unreleased]` section exists.
  - Artifact text: "The entry lands under a `### Breaking` heading, matching the existing convention at `CHANGELOG.md:12`." (`files: [CHANGELOG.md]`, `is_wiring_task: true`, `model_hint: cheap`, `review_mode: merged`)
  - Evidence: `CHANGELOG.md:10` = `## [0.4.0] - 2026-07-28`; `:12` = that section's `### Breaking`. Two independent sweeps for an `[Unreleased]` home: (a) heading scan `^## ` over `CHANGELOG.md` returns only `[0.4.0]`, `[0.3.1]`, `[0.3.0]`, `[0.1.0]` — no `[Unreleased]`; (b) `RELEASING.md:13-14` documents the intended flow — "move the relevant notes from `[Unreleased]` into a new `## [x.y.z] - YYYY-MM-DD` section" — so the section is expected to exist and currently does not.
  - Concrete failure: the implementer sees one file, one anchor, and a `cheap` model hint. The literal reading is "append to the `### Breaking` list at line 12", which files a `CliContext` break under the published 0.4.0 release — the changelog then asserts that 0.4.0 shipped a breaking change it did not ship, and the next release cut per `RELEASING.md:13` finds nothing under `[Unreleased]` to move. The alternative reading (invent `## [Unreleased]`) makes AC 1's `:12` citation false, so a `review_mode: merged` reviewer holding the task body as binding spec accepts the wrong one either way. Nothing downstream corrects it — this task's output *is* the artifact.
  - Resolution: undecidable as written — the author must choose between (A) "create a new `## [Unreleased]` section immediately above line 10, with `### Breaking` and `### Added` subheadings, and cite `RELEASING.md:13-14` as the reason" and (B) "append under the existing 0.4.0 `### Breaking`". Whichever is chosen must be stated in the task body, since the implementer has no other source.

- **BLOCKING** · `task-bin-spawn`'s mutation criterion requires an edit outside its declared file scope, omits the rebuild it depends on, and steers toward the one repair the plan forbids — a prohibition that never reaches this task.
  - Artifact text: "Removing `getSyncProviders` from the `buildProgram` call at `src/index.ts:110` makes this test fail. Verify by hand once during implementation; do not commit the broken state." — under `files:` containing only `packages/pangolin-cli/test/bin-spawn.test.ts`.
  - Evidence: the construction site is `packages/pangolin-cli/src/index.ts:110`, not in this task's `files:`. The test spawns built output — `join(__dirname, '../dist/index.js')` — and `packages/pangolin-cli/package.json` builds with `"build": "tsc"` against `tsconfig.json` (`outDir: dist`, `rootDir: src`), so `dist/index.js` only reflects a `src` edit after a rebuild; the task never says to rebuild. `tsconfig.base.json` sets no `noEmitOnError`, so `tsc` emits despite the `TS2345` that removing a required member produces, but exits non-zero. The prohibition on the optional-member repair exists only in `## Context` guard 2 ("do **not** declare `getSyncProviders` optional and do **not** call it with `?.`") and is inlined into **`task-actions`** alone (its implementation comment and AC 2) — `task-bin-spawn`'s body contains no trace of it. Charter tell: `.claude/audit-charter.md:54-59` ("green tests, dead runnable artifact" — the entrypoint is constructed differently by tests than by production).
  - Concrete failure, three ways: (a) the implementer honours their `files:` list, skips the criterion, and the plan's only end-to-end proof that `:110` is wired is never actually performed — restoring exactly the round-1 B2 defect the task exists to close; (b) they mutate `src/index.ts`, re-run the spawn without rebuilding, observe the *stale* `dist/index.js` behave unchanged, and conclude the guard is sound when it proved nothing — a vacuous mutation check, and then must revert a file they were not scoped to touch; (c) they mutate, hit `TS2345` on `pnpm build`, and reach for the natural repair — `getSyncProviders?:` or `ctx.getSyncProviders?.(…)` — with nothing in their task telling them that is forbidden. Under (c) the compiler guard is deleted and `:110` can be unwired silently, which is the failure mode the entire spec revision was written to prevent.
  - Resolution: either add `packages/pangolin-cli/src/index.ts` to this task's `files:` and spell the sequence out (`mutate → pnpm --filter @quarry-systems/pangolin-cli build → run test → expect red → revert → rebuild → expect green`), inlining guard 2's prohibition verbatim; or delete the manual criterion and replace it with a check executable from inside the test file alone.

- **DEFERRED** · `task-actions` instructs the implementer to read a document section the execution contract never gives them, and its compensating check is not runnable as typed.
  - Artifact text: "Re-read scope guards 1 and 2 in `## Context` before touching any test file." and, in AC 5, "`grep -c \"getSyncProviders\" packages/pangolin-cli/test/` counts exactly the two files in this task's `files:`".
  - Evidence: the guards live at plan lines 52-78, inside `## Context`, which no implementer receives. Their *substance* does survive into this task — guard 1 via the two-file `files:` list plus AC 5, guard 2 via the implementation comment "`// Widen it. Do NOT make the member optional and do NOT use ?. (scope guard 2)`" plus AC 2 ("proving the fixture was widened rather than the member weakened"). What does not survive is the rationale (44 sites / 12 files / no `tsconfig.test.json` at `packages/pangolin-cli/tsconfig.json:6`; `:110` unreachable from vitest per `src/index.ts:109`). Separately, `grep -c <pattern> <directory>` without `-r` does not recurse — it reports the path as a directory and exits non-zero — so AC 5 cannot be executed as written by either implementer or reviewer.
  - Concrete failure: a rationale-free prohibition holds under normal conditions but is the first thing discarded under pressure, and the implementer cannot tell a reviewer *why* the ten untouched fixtures are acceptable rather than an oversight. The unrunnable grep means the guard-1 check is likely to be skipped or silently reinterpreted. Neither is fatal here because the rule text itself is present; see the BLOCKING finding above for the task where the rule text is absent.
  - Resolution: inline one sentence of guard-2 rationale ("the compiler is the only guard: `:110` is inside `require.main === module` and vitest never executes it") and fix AC 5 to `grep -rl "getSyncProviders" packages/pangolin-cli/test/` returning exactly two paths.

- **DEFERRED** · `task-config-loader` elides two of the four error literals its own criterion says must be written into the test, defeating the criterion's purpose.
  - Artifact text: "`defaultGetClient` and `defaultGetOrchContext` throw their four **existing** message strings unchanged. Assert them against literals written into the test, not imported from source: … and the two `orch` equivalents."
  - Evidence: the four literals are at `packages/pangolin-cli/src/index.ts:63, 68, 88, 93`. The task inlines two of them; the `orch` pair — `pangolin-cli: <filename> must export an OrchContext as a named 'orch' export for pangolin orch commands` (`:88`) and the duplicate no-config message (`:93`) — is named only as "the two `orch` equivalents".
  - Concrete failure: the criterion exists so the test pins the *pre-refactor* strings rather than whatever the refactor produces. The implementer is refactoring that exact file and has no other source for the two orch strings, so they will copy them out of the file — very plausibly after editing it — and the pin becomes a tautology. The reviewer, holding only the task body, has nothing to compare the two orch assertions against and cannot detect a drifted string.
  - Resolution: paste all four literals verbatim into the task body.

- **DEFERRED** · `task-config-loader`'s `.ts`-leg criterion is a bare pointer to a spec section the implementer cannot open, and the fallback it offers silently changes what gets tested.
  - Artifact text: "Resolution order `.ts` → `.js` → `.mjs` holds for all three `defaultGet*` functions (`.ts` leg per the spec's EU1 caveat — assert `.js` before `.mjs` at minimum)."
  - Evidence: EU1's content is at `docs/superpowers/specs/2026-07-29-sync-provider-spi-design.md:669-683` — Node ≥22.18 strips types and resolves `pangolin.config.ts`, while the repo's declared floor `"node": ">=20"` (`package.json:8-10`) fails with `ERR_UNKNOWN_FILE_EXTENSION`; CI pins `node-version: '22'` (`.github/workflows/ci.yml:25`), so CI cannot see the difference. The spec's own closing note (`:680-683`) says the unknown "must be given [an owning task] when the plan is written"; a scan of all eight task bodies in the plan finds no probe task and no other mention of EU1 (searches: literal `EU1` across the plan → one hit, this criterion; `files:` union across all tasks → no probe artifact).
  - Concrete failure: with no content behind the pointer, the implementer either writes a `.ts` assertion — green on CI's Node 22, red for any contributor on the declared Node 20 floor, an environment CI structurally cannot detect — or drops the `.ts` leg entirely, leaving the first resolution leg unpinned in the very task that rewrites the resolution loop. Both look compliant to a reviewer reading only the task body.
  - Resolution: inline the caveat as one sentence plus an explicit instruction ("assert `.js` before `.mjs`; do **not** assert the `.ts` leg — it is version-dependent"), or add the probe task the spec asks for.

- **DEFERRED** · `task-docs`' provisional-stability criterion has no destination file, no drafted content, and no reachable source.
  - Artifact text: "The provisional-stability note names the `./providers` subpath specifically, and gives `loadSubagents(dir)`'s two conflicting meanings as the reason."
  - Evidence: the task's Implementation block sketches only `sync-capabilities-subagents.md` and `config.md`, and neither sketch contains any stability note; `cli.md` gets no sketch at all. The "two conflicting meanings" are real but stated only in spec §8 (`…sync-provider-spi-design.md:615-621`) and, implicitly, in source at `packages/pangolin-cli/src/providers/claude-code.ts:28,31-32` (`defaultSubagentDir = '.claude/agents'`, `readdir(dir)` directly) versus `packages/pangolin-cli/src/providers/stoa.ts:33,38-40` (`defaultSubagentDir = '.'`, `loadSubagents(repoRoot)` joining two subdirs). Nothing in the task points at either file.
  - Concrete failure: the implementer must guess which of three files hosts the note and must reverse-engineer or invent the two meanings. A quality reviewer holding only the task body cannot tell whether the two meanings written down are the right two, so the criterion is unverifiable at review — and `pnpm --filter docs-site build`, the task's only mechanical check, verifies links, not prose.
  - Resolution: name the destination file and inline the two sentences (one per provider, with the `file:line` citations above).

- **DEFERRED** · `task-docs` AC 6 names a `cli.md` row that does not exist, and the real second row will plausibly be missed.
  - Artifact text: "`cli.md` rows for `sync` and `--provider` both state that the value may name a config-supplied provider and that built-in names cannot be overridden."
  - Evidence: `docs-site/src/content/docs/reference/cli.md:37` is the `sync` row under `## pangolin capabilities`; `:49` is the `sync` row under `## pangolin subagent`. Two searches confirm there is no `--provider` row: (a) grep for `--provider` in `cli.md` returns exactly those two lines; (b) a table scan of lines 30-55 shows both sections list subcommands as rows, with flags inside the options cell, never as their own row.
  - Concrete failure: the implementer looks for a `--provider` row, does not find one, updates the single `sync` row they did find (most likely `:37`, the first), and stops. The subagent `sync` row at `:49` keeps describing `--provider` as selecting among built-in adapters — the stale claim this task exists to remove. AC 6 as written is satisfied by that one edit, so the reviewer passes it.
  - Resolution: replace with "both `sync` rows — `cli.md:37` (capabilities) and `cli.md:49` (subagent)".

- **DEFERRED** · `task-exports`' probe cannot resolve the subpath from the location the task tells the implementer to write it, and the task's file scope offers no stated alternative.
  - Artifact text: "`const probe = join(await mkdtemp(join(tmpdir(), 'pangolin-spi-')), 'probe.mjs');` … `const { stdout } = await run(process.execPath, [probe], { cwd: pkgRoot });`" with `files: [packages/pangolin-cli/package.json, packages/pangolin-cli/test/exports-resolution.test.ts]`.
  - Evidence: ESM bare-specifier resolution is anchored on the importing file's URL, not on `cwd`, and package self-reference requires the importer to be inside the package directory. Two independent checks that no `node_modules` path serves a tmpdir importer: (a) `ls node_modules/@quarry-systems/` at the repo root → "No such file or directory"; (b) `ls packages/pangolin-cli/node_modules/@quarry-systems/` → only `pangolin-client`, `pangolin-core`, `pangolin-orchestrator`, `pangolin-product` (its declared workspace deps), no self-link. `pkgRoot` is additionally never defined in the sketch, and the imports (`run`, `mkdtemp`, `tmpdir`, `join`, `writeFile`) are omitted.
  - Concrete failure: the sketch fails with `ERR_MODULE_NOT_FOUND` regardless of whether the `exports` map is correct. The implementer must invent a placement the `files:` list does not contemplate (writing the probe inside `packages/pangolin-cli/` and cleaning it up is the working option). The recovery path with the worst outcome is the one the same criterion explicitly forbids — falling back to an in-process `await import()`, which "would prove nothing" — and an implementer who cannot see the spec has only that one forbidding sentence to hold them back.
  - Resolution: state that the probe `.mjs` must be written *inside* `packages/pangolin-cli/` (self-reference is what makes the specifier resolve), define `pkgRoot = resolve(__dirname, '..')` per the existing idiom at `packages/pangolin-cli/test/scaffold-shape.test.ts:6-8`, and note the cleanup.

### Checked, no finding

- **The serial spine is genuine, not incidental.** `task-registry → task-config-loader → task-actions → task-barrel` cannot be parallelized further as scoped: `src/cmd-subagent.ts:20` and `src/cmd-capabilities.ts:21` import `resolveProvider` from `./providers/index.js`, so the barrel cannot be trimmed before the actions move, and the actions cannot call `ctx.getSyncProviders` before `CliContext` declares it. The `## Context` justification is accurate.
- **No task is too large for one implement-and-review pass.** `task-registry` is the biggest (one ~90-line module, one test, 8 criteria) and is fully self-contained: the entire module body is inlined, so no spec reading is required. `task-config-loader` and `task-docs` are the widest review surfaces but stay coherent.
- **Guard 2 does reach `task-actions`.** The prohibition text is inlined twice (implementation comment and AC 2) and the required repair direction ("Widen the fixture") is explicit. The `## Context` placement costs rationale, not rule — the failure is confined to `task-bin-spawn` (BLOCKING finding above).
- **Guard 1 and guard 3 are load-bearing only through file scope, and file scope carries them.** `task-actions`' `files:` names exactly the two test files guard 1 permits; no task's `files:` includes anything under `packages/pangolin-mcp/`, so guard 3 cannot be violated without an out-of-scope edit.
- **`task-registry` and `task-barrel` are context-complete.** Both inline the entire target file. Every acceptance criterion in `task-registry` is checkable against the inlined module body alone; `task-barrel`'s whole-set `Object.keys` equality is verifiable from its own snippet. Neither needs the spec.
- **The import-safety requirement — the one thing spec §10 says only the docs will convey — is fully inlined** in `task-docs`' implementation block, in all three parts (real `dependency`, survives a pruned production install, no module-scope throw) with `pangolin-mcp` startup named as the reason. This is the plan's best-carried spec obligation.
- **`task-changelog`'s cross-task knowledge is thin but adequate.** It forks off `task-config-loader` and must describe the `./providers` subpath and the `syncProviders` config export — artifacts of `task-exports` and `task-registry`, which it neither depends on nor can see. Both names appear literally in its own body and AC 4, so a correct if terse entry is writable. Not a finding; the placement problem (F1) is the real defect here.
- **`task-config-loader`'s fixture mechanism is discoverable.** The `// fixture cwd contains only pangolin.config.js` comment implies `process.chdir`, for which prior art exists in the same directory at `packages/pangolin-cli/test/cmd-deploy.test.ts:24,84,116,158,200,233,257`.
- **`task-bin-spawn`'s pattern pointer is mildly wrong but self-correcting.** "Mirror the existing spawn pattern in `test/e2e/mcp-tool-surface.test.ts`" reads as package-relative; the file is actually at repo root (`./test/e2e/mcp-tool-surface.test.ts`, confirmed by `find`), not under `packages/pangolin-cli/test/`. One `find` resolves it. Noted, not filed.

### Out of lens

- `examples/manifest/test/deploy.test.ts:145` is a thirteenth `CliContext` construction site outside the "12 test files" the `## Context` guard counts. It is inert (no `examples/manifest/package.json`, so `pnpm -r` skips it; `vitest.e2e.config.ts` includes only repo-root `test/**`), but it will silently rot and no task owns it.
- `packages/pangolin-cli/test/providers/` already exists (`claude-code.test.ts`, `stoa.test.ts`); `task-registry` places its test at `test/providers-registry.test.ts` rather than `test/providers/registry.test.ts`, diverging from the established layout.
