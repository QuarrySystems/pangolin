# Claude credential for the worker — set EXACTLY ONE.
#
# API credits (bills your Anthropic API org per token):
ANTHROPIC_API_KEY=
#
# OR a Claude Pro/Max subscription (no API credits). Mint once on a machine with
# a browser: `claude setup-token`, then paste the sk-ant-oat01-... token here.
# Auto-detected; when both are set the subscription wins. Force a lane with
# PANGOLIN_CLAUDE_AUTH=subscription|api-key.
# CLAUDE_CODE_OAUTH_TOKEN=
