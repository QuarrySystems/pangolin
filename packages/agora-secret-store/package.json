{
  "name": "@quarry-systems/agora-secret-store",
  "version": "0.1.0",
  "license": "BUSL-1.1",
  "description": "SecretStore seam plus implementations — InlineSecretStager (AWS Secrets Manager) and LocalSecretStore (on-disk staging) for per-dispatch inline secrets.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/QuarrySystems/agora.git",
    "directory": "packages/agora-secret-store"
  },
  "homepage": "https://quarrysystems.github.io/agora",
  "bugs": {
    "url": "https://github.com/QuarrySystems/agora/issues"
  },
  "scripts": {
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@quarry-systems/agora-core": "workspace:*"
  },
  "devDependencies": {}
}
