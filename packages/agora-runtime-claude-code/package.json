{
  "name": "@quarry-systems/agora-runtime-claude-code",
  "version": "0.0.0",
  "license": "BUSL-1.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc && node -e \"require('node:fs').cpSync('src/assets', 'dist/assets', { recursive: true })\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@quarry-systems/agora-core": "workspace:*",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@types/mustache": "^4.2.0"
  }
}
