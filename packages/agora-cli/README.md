# @quarry-systems/agora-cli

The CLI for agora. Provides the `agora` binary.

## Status

This is a scaffold-only package. Full implementation lands in **DAG 2**.

The `agora` binary entry point (`dist/index.js`) is declared but empty in this phase.

## Future scope (DAG 2)

- Subcommands for interacting with the agora server
- Configuration management
- Session and coordination tooling
